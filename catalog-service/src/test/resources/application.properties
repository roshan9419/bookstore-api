spring.test.database.replace=none

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/bookstore-catalog-test}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}